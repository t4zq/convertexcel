# converTeXcel VS Code Extension

Insert LaTeX table or TikZ/PGFPlots code from a converTeXcel share URL.

## Commands

- `converTeXcel: Insert LaTeX Table from Share URL`
- `converTeXcel: Insert TikZ Graph from Share URL`

The extension reads the selected text first. If the selection does not contain a converTeXcel share URL, it reads the clipboard.
